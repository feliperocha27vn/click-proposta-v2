/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import type {
  GetPublicBudgetQueryResponse,
  GetPublicBudgetPathParams,
  GetPublicBudget404,
} from "../../types/GetPublicBudget.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getPublicBudgetQueryKey = (
  budgetId: GetPublicBudgetPathParams["budgetId"],
) =>
  [
    { url: "/public/budgets/:budgetId", params: { budgetId: budgetId } },
  ] as const;

export type GetPublicBudgetQueryKey = ReturnType<
  typeof getPublicBudgetQueryKey
>;

/**
 * @description Fetch budget details for public viewing without authentication.
 * @summary Get public budget details
 * {@link /public/budgets/:budgetId}
 */
export async function getPublicBudget(
  budgetId: GetPublicBudgetPathParams["budgetId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetPublicBudgetQueryResponse,
    ResponseErrorConfig<GetPublicBudget404>,
    unknown
  >({ method: "GET", url: `/public/budgets/${budgetId}`, ...requestConfig });
  return res.data;
}

export function getPublicBudgetQueryOptions(
  budgetId: GetPublicBudgetPathParams["budgetId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getPublicBudgetQueryKey(budgetId);
  return queryOptions<
    GetPublicBudgetQueryResponse,
    ResponseErrorConfig<GetPublicBudget404>,
    GetPublicBudgetQueryResponse,
    typeof queryKey
  >({
    enabled: !!budgetId,
    queryKey,
    queryFn: async ({ signal }) => {
      return getPublicBudget(budgetId, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @description Fetch budget details for public viewing without authentication.
 * @summary Get public budget details
 * {@link /public/budgets/:budgetId}
 */
export function useGetPublicBudget<
  TData = GetPublicBudgetQueryResponse,
  TQueryData = GetPublicBudgetQueryResponse,
  TQueryKey extends QueryKey = GetPublicBudgetQueryKey,
>(
  budgetId: GetPublicBudgetPathParams["budgetId"],
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetPublicBudgetQueryResponse,
        ResponseErrorConfig<GetPublicBudget404>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getPublicBudgetQueryKey(budgetId);

  const query = useQuery(
    {
      ...getPublicBudgetQueryOptions(budgetId, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<GetPublicBudget404>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
