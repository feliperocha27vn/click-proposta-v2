/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import type {
  GetByIdBudgetQueryResponse,
  GetByIdBudgetPathParams,
  GetByIdBudget404,
} from "../../types/GetByIdBudget.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getByIdBudgetQueryKey = (
  budgetId: GetByIdBudgetPathParams["budgetId"],
) => [{ url: "/budgets/:budgetId", params: { budgetId: budgetId } }] as const;

export type GetByIdBudgetQueryKey = ReturnType<typeof getByIdBudgetQueryKey>;

/**
 * {@link /budgets/:budgetId}
 */
export async function getByIdBudget(
  budgetId: GetByIdBudgetPathParams["budgetId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetByIdBudgetQueryResponse,
    ResponseErrorConfig<GetByIdBudget404>,
    unknown
  >({ method: "GET", url: `/budgets/${budgetId}`, ...requestConfig });
  return res.data;
}

export function getByIdBudgetQueryOptions(
  budgetId: GetByIdBudgetPathParams["budgetId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getByIdBudgetQueryKey(budgetId);
  return queryOptions<
    GetByIdBudgetQueryResponse,
    ResponseErrorConfig<GetByIdBudget404>,
    GetByIdBudgetQueryResponse,
    typeof queryKey
  >({
    enabled: !!budgetId,
    queryKey,
    queryFn: async ({ signal }) => {
      return getByIdBudget(budgetId, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /budgets/:budgetId}
 */
export function useGetByIdBudget<
  TData = GetByIdBudgetQueryResponse,
  TQueryData = GetByIdBudgetQueryResponse,
  TQueryKey extends QueryKey = GetByIdBudgetQueryKey,
>(
  budgetId: GetByIdBudgetPathParams["budgetId"],
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetByIdBudgetQueryResponse,
        ResponseErrorConfig<GetByIdBudget404>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getByIdBudgetQueryKey(budgetId);

  const query = useQuery(
    {
      ...getByIdBudgetQueryOptions(budgetId, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<GetByIdBudget404>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
