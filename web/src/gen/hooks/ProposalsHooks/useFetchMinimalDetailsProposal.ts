/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import type {
  FetchMinimalDetailsProposalQueryResponse,
  FetchMinimalDetailsProposal500,
} from "../../types/FetchMinimalDetailsProposal.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const fetchMinimalDetailsProposalQueryKey = () =>
  [{ url: "/proposals/minimal-details" }] as const;

export type FetchMinimalDetailsProposalQueryKey = ReturnType<
  typeof fetchMinimalDetailsProposalQueryKey
>;

/**
 * {@link /proposals/minimal-details}
 */
export async function fetchMinimalDetailsProposal(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    FetchMinimalDetailsProposalQueryResponse,
    ResponseErrorConfig<FetchMinimalDetailsProposal500>,
    unknown
  >({ method: "GET", url: `/proposals/minimal-details`, ...requestConfig });
  return res.data;
}

export function fetchMinimalDetailsProposalQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = fetchMinimalDetailsProposalQueryKey();
  return queryOptions<
    FetchMinimalDetailsProposalQueryResponse,
    ResponseErrorConfig<FetchMinimalDetailsProposal500>,
    FetchMinimalDetailsProposalQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return fetchMinimalDetailsProposal({
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /proposals/minimal-details}
 */
export function useFetchMinimalDetailsProposal<
  TData = FetchMinimalDetailsProposalQueryResponse,
  TQueryData = FetchMinimalDetailsProposalQueryResponse,
  TQueryKey extends QueryKey = FetchMinimalDetailsProposalQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        FetchMinimalDetailsProposalQueryResponse,
        ResponseErrorConfig<FetchMinimalDetailsProposal500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? fetchMinimalDetailsProposalQueryKey();

  const query = useQuery(
    {
      ...fetchMinimalDetailsProposalQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<FetchMinimalDetailsProposal500>
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
