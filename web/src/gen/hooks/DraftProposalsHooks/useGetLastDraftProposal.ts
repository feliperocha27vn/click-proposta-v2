/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import type {
  GetLastDraftProposalQueryResponse,
  GetLastDraftProposalPathParams,
  GetLastDraftProposal404,
  GetLastDraftProposal500,
} from "../../types/GetLastDraftProposal.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getLastDraftProposalQueryKey = (
  customerId: GetLastDraftProposalPathParams["customerId"],
) =>
  [
    {
      url: "/draft-proposals/:customerId/last",
      params: { customerId: customerId },
    },
  ] as const;

export type GetLastDraftProposalQueryKey = ReturnType<
  typeof getLastDraftProposalQueryKey
>;

/**
 * {@link /draft-proposals/:customerId/last}
 */
export async function getLastDraftProposal(
  customerId: GetLastDraftProposalPathParams["customerId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetLastDraftProposalQueryResponse,
    ResponseErrorConfig<GetLastDraftProposal404 | GetLastDraftProposal500>,
    unknown
  >({
    method: "GET",
    url: `/draft-proposals/${customerId}/last`,
    ...requestConfig,
  });
  return res.data;
}

export function getLastDraftProposalQueryOptions(
  customerId: GetLastDraftProposalPathParams["customerId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getLastDraftProposalQueryKey(customerId);
  return queryOptions<
    GetLastDraftProposalQueryResponse,
    ResponseErrorConfig<GetLastDraftProposal404 | GetLastDraftProposal500>,
    GetLastDraftProposalQueryResponse,
    typeof queryKey
  >({
    enabled: !!customerId,
    queryKey,
    queryFn: async ({ signal }) => {
      return getLastDraftProposal(customerId, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /draft-proposals/:customerId/last}
 */
export function useGetLastDraftProposal<
  TData = GetLastDraftProposalQueryResponse,
  TQueryData = GetLastDraftProposalQueryResponse,
  TQueryKey extends QueryKey = GetLastDraftProposalQueryKey,
>(
  customerId: GetLastDraftProposalPathParams["customerId"],
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetLastDraftProposalQueryResponse,
        ResponseErrorConfig<GetLastDraftProposal404 | GetLastDraftProposal500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getLastDraftProposalQueryKey(customerId);

  const query = useQuery(
    {
      ...getLastDraftProposalQueryOptions(customerId, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<GetLastDraftProposal404 | GetLastDraftProposal500>
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
