/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import type {
  GetLastDraftProposalQueryResponse,
  GetLastDraftProposalPathParams,
  GetLastDraftProposal404,
  GetLastDraftProposal500,
} from "../../types/GetLastDraftProposal.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getLastDraftProposalSuspenseQueryKey = (
  customerId: GetLastDraftProposalPathParams["customerId"],
) =>
  [
    {
      url: "/draft-proposals/:customerId/last",
      params: { customerId: customerId },
    },
  ] as const;

export type GetLastDraftProposalSuspenseQueryKey = ReturnType<
  typeof getLastDraftProposalSuspenseQueryKey
>;

/**
 * {@link /draft-proposals/:customerId/last}
 */
export async function getLastDraftProposalSuspense(
  customerId: GetLastDraftProposalPathParams["customerId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetLastDraftProposalQueryResponse,
    ResponseErrorConfig<GetLastDraftProposal404 | GetLastDraftProposal500>,
    unknown
  >({
    method: "GET",
    url: `/draft-proposals/${customerId}/last`,
    ...requestConfig,
  });
  return res.data;
}

export function getLastDraftProposalSuspenseQueryOptions(
  customerId: GetLastDraftProposalPathParams["customerId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getLastDraftProposalSuspenseQueryKey(customerId);
  return queryOptions<
    GetLastDraftProposalQueryResponse,
    ResponseErrorConfig<GetLastDraftProposal404 | GetLastDraftProposal500>,
    GetLastDraftProposalQueryResponse,
    typeof queryKey
  >({
    enabled: !!customerId,
    queryKey,
    queryFn: async ({ signal }) => {
      return getLastDraftProposalSuspense(customerId, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /draft-proposals/:customerId/last}
 */
export function useGetLastDraftProposalSuspense<
  TData = GetLastDraftProposalQueryResponse,
  TQueryKey extends QueryKey = GetLastDraftProposalSuspenseQueryKey,
>(
  customerId: GetLastDraftProposalPathParams["customerId"],
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetLastDraftProposalQueryResponse,
        ResponseErrorConfig<GetLastDraftProposal404 | GetLastDraftProposal500>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ??
    getLastDraftProposalSuspenseQueryKey(customerId);

  const query = useSuspenseQuery(
    {
      ...getLastDraftProposalSuspenseQueryOptions(customerId, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<
    TData,
    ResponseErrorConfig<GetLastDraftProposal404 | GetLastDraftProposal500>
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
