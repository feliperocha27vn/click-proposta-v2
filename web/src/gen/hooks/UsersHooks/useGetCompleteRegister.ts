/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import type {
  GetCompleteRegisterQueryResponse,
  GetCompleteRegister404,
  GetCompleteRegister500,
} from "../../types/GetCompleteRegister.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getCompleteRegisterQueryKey = () =>
  [{ url: "/is-complete-register" }] as const;

export type GetCompleteRegisterQueryKey = ReturnType<
  typeof getCompleteRegisterQueryKey
>;

/**
 * {@link /is-complete-register}
 */
export async function getCompleteRegister(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetCompleteRegisterQueryResponse,
    ResponseErrorConfig<GetCompleteRegister404 | GetCompleteRegister500>,
    unknown
  >({ method: "GET", url: `/is-complete-register`, ...requestConfig });
  return res.data;
}

export function getCompleteRegisterQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getCompleteRegisterQueryKey();
  return queryOptions<
    GetCompleteRegisterQueryResponse,
    ResponseErrorConfig<GetCompleteRegister404 | GetCompleteRegister500>,
    GetCompleteRegisterQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getCompleteRegister({
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /is-complete-register}
 */
export function useGetCompleteRegister<
  TData = GetCompleteRegisterQueryResponse,
  TQueryData = GetCompleteRegisterQueryResponse,
  TQueryKey extends QueryKey = GetCompleteRegisterQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetCompleteRegisterQueryResponse,
        ResponseErrorConfig<GetCompleteRegister404 | GetCompleteRegister500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getCompleteRegisterQueryKey();

  const query = useQuery(
    {
      ...getCompleteRegisterQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<GetCompleteRegister404 | GetCompleteRegister500>
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
